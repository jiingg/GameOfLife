import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        Map startMap = new Map();

        startMap.setMap(sc.nextInt(), sc.nextInt(), true);
    }
}
